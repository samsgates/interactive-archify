# Local development

```bash
pnpm install
cp .env.example .env.local
pnpm build
pnpm dev
```

The web app starts through Next.js. For CLI use, build once and then run `pnpm cli -- help` or link the CLI package.
